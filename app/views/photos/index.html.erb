<h1>Photo Index</h1>

<div class="photos">
  <div class="container">

<% @photos.each do |photo| %>
<div class="photo">
  <%= photo.title %>
  <%= image_tag photo.image.url(:medium) %>
</div>
<% end %>

  </div>
</div>
